# Cyber Theme

A dark neon cyberpunk theme with cyan glow and scan-line effects.

**Type:** Theme  
**Entry:** `themes/variables.css`  
**Hook:** `ui:css-variables`

Overrides all UI colours via CSS custom properties when `data-theme="cyber"` is applied.