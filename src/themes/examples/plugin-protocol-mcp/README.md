# MCP Protocol

A Model Context Protocol service registration and discovery plugin.

**Type:** Protocol  
**Entry:** `protocol.json`  
**Hook:** `protocol:register`

Defines three endpoints: `register`, `discover`, and `invoke` for MCP service lifecycle.