# Web Search Tool

A tool plugin that searches the web and retrieves page content.

**Type:** Tool  
**Entry:** `tool_def.json`  
**Hook:** `tool:execute`

Defines a `web_search` tool with `query` and optional `max_results` parameters.