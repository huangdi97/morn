# WeChat Channel

A WeCom (企业微信) bot channel plugin supporting message receive/send and task management.

**Type:** Channel  
**Entry:** `channel.js`  
**Hooks:** `channel:message`, `channel:send`

Exports `WeChatChannel` class with `receiveMessage` and `sendMessage` methods.